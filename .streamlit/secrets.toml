# .streamlit/secrets.toml
# Configuration overrides for LOCAL development
# IMPORTANT: Add this file to your .gitignore!

# Lower limits for easier local testing
MAX_AUDIO_DURATION_S = 300  # 5 minutes locally
MAX_TRACKS = 5              # Only 5 tracks locally
MAX_AFFIRMATION_CHARS = 500 # Shorter affirmations locally

# Example: Maybe you want shorter previews locally
PREVIEW_DURATION_S = 5
MIX_PREVIEW_DURATION_S = 8

# Keep other values as default by omitting them,
# or specify them if needed.
GLOBAL_SR = 44100 # Usually keep this consistent
